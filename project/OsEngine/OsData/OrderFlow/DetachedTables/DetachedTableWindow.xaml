<Window x:Class="OsEngine.OsData.OrderFlow.DetachedTables.DetachedTableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Таблица" Width="1100" Height="650" MinWidth="480" MinHeight="360"
        WindowStartupLocation="CenterScreen" Style="{DynamicResource WindowStyleCanResize}">
    <Grid Margin="8" Background="{DynamicResource ControlBackgroundNormalLight}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel Margin="0,0,0,6">
            <Button Name="ButtonSource" DockPanel.Dock="Right" Content="К исходному экрану" Height="Auto" MinHeight="28" Padding="8,4" Margin="4,0"
                    ToolTip="Вернуться к экрану, которому принадлежит эта таблица. Данные и расчёт не меняются."/>
            <TextBlock Name="TextBlockContext" TextWrapping="Wrap" Foreground="{DynamicResource ControlForegroundWhite}" VerticalAlignment="Center"/>
        </DockPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="180">
            <ContentControl Name="ContentControlTools" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
        </ScrollViewer>
        <ContentControl Name="ContentControlTable" Grid.Row="2" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
        <TextBlock Name="TextBlockStatus" Grid.Row="3" Margin="4,6" TextWrapping="Wrap" Foreground="{DynamicResource ControlForegroundWhite}"/>
    </Grid>
</Window>
