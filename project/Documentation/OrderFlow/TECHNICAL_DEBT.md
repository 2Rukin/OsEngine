# ORDER-FLOW-TECH-DEBT-001: открытые проблемы Cloud Explorer и история окон

**Статус:** CURRENT ISSUE REGISTER — четыре исправленные записи ожидают ручной проверки окна, две записи закрыты.

Формулировки ниже основаны на наблюдениях владельца от 24.09.2026 и сверке с
current code. Скриншоты не являются выполненными автоматическими тестами
настоящего Windows/WPF окна. Полные ожидаемое поведение, варианты ввода и
проверки — в [следующей постановке](CLOUD_EXPLORER_FOLLOWUP_SPEC.md).

## Исправлены в коде — ручная проверка окна ещё требуется

Реализация относительно `9a6aaa351` заменяет общую проверку адресной русской
валидацией, закрепляет сводку сверху, вводит единый временной срез графика и
отдельный масштаб Y, пояснения эпизодов и независимые страницы таблиц.
Автоматические fixtures и границы проверки описаны в
[qualification](TESTING_AND_QUALIFICATION.md#поиск-предвестников-и-доработки-explorer).
Исторические симптомы в таблице сохранены для ручного повторения владельцем;
это не утверждение о наличии прежнего кода в текущей реализации.

| ID | Симптом владельца и подтверждённая граница кода | Требуемое исправление |
|---|---|---|
| `TD-CLOUD-VALIDATION-001` | После перестановки дат кнопка «3.1 Рассчитать каталог / исследование» выдала `ArgumentException: Invalid Cloud Explorer input, dates, profiles or resource limit.` со stack trace; в `ExplorerRunSpec.Validate` несколько разных проверок действительно сведены к одной английской фразе | Указать конкретное поле, причину и действие по-русски. Охватить все ветви ввода, reader, хранилища и реплея; системную диагностику оставить в логе. Матрица `2` постановки |
| `TD-CLOUD-SUMMARY-001` | На вкладке «Сводка» видна только строка в большой пустой области; остальные строки обнаруживаются прокруткой. Код формирует многострочный `TextBox` и вызывает `AppendText`, но причину точного расположения строки на экране пока не проверяли | Сразу показывать заголовок и первые блоки, обеспечить заметную прокрутку и читаемую группировку при изменении размеров окна. `3.1` постановки |
| `TD-CLOUD-CHART-001` | Нет вертикального масштаба. На графике жёлтые точки растянуты по времени, а свечи оказались на узком участке; в коде горизонтальный диапазон включает pivots из иной вспомогательной страницы, свечи загружены для текущей страницы Cloud | Независимое управление ценовой осью, согласованный диапазон свечей/меток, легенда экстремумов; проверить страницы, отдельное окно и реплей. `3.3` постановки |
| `TD-CLOUD-EPISODES-001` | «Эпизоды» показывают заголовки без строк и не сообщают причину. По коду модуль выключен по умолчанию (`Enabled=false`); отсутствие строк в этом случае ожидаемо, но не объяснено пользователю | Явно различать выключенный модуль, отсутствие найденных эпизодов и конец страниц; показать способ включения и численность. `3.2` постановки |

## Закрытые записи

### TD-CLOUD-GUIDE-001 — пояснения и примеры к текущему руководству

**Состояние:** CLOSED — документационная доработка в рамках
`TASK-CLOUD-EXPLORER-FOLLOWUP-001`.

Ранее руководство перечисляло настройки, но не показывало, какие комбинации
выбирать для вопроса об отдельных сделках, цепочках, эпизодах и будущих
исходах. Теперь в [руководстве](CLOUD_EXPLORER_USER_GUIDE.md#33-что-включено-в-первом-запуске-и-зачем-параметры-комбинировать)
есть карта параметров, причины пустых разделов и пример вывода с Fit/Test.
Доработки поведения интерфейса и отдельный автоматический поиск реализованы
в рабочем diff следующей задачи; ручная проверка реального окна остаётся
отдельной от offline component evidence.

## TD-CLOUD-WINDOW-001 — ввод в Order Flow при открытом Cloud

**Состояние:** CLOSED — повторная ручная проверка владельца, 24.09.2026.
**Область:** OsData → Order Flow → «Исследование Cloud», переключение и ввод.

После первоначальных изменений владелец сообщал, что окно Cloud остаётся
поверх Order Flow и мешает вводу. На основании того сообщения запись была
отложена. После коммита `180e830ea65f1b3b2072236e5575fe38c0ffebba`
владелец повторил проверку и сообщил, что всё работает. Поэтому прежняя
жалоба больше не считается открытым дефектом, а временный совет закрывать
Cloud перед изменением входных параметров отменён.

В опубликованном коде Order Flow открывается через `Show()`, Cloud и вынесенным
графикам не назначается WPF `Owner`, а принудительный `Topmost` удалён из
затронутых XAML-окон. Точная причина первоначального наблюдения не установлена;
по исходному сообщению нельзя отдельно доказать, какое изменение его устранило.

Сборка решения и offline-тесты 144/144 в предыдущем коммите проверяли код,
но не ввод в настоящие окна ОС. Основание закрытия этой записи — **повторная
ручная проверка владельца**. Состав дополнительных проверок (OsData, внешнее
приложение, повторное открытие и закрытие окон) не зафиксирован и здесь не
объявляется пройденным.
